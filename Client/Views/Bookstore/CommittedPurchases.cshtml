@model List<Client.Models.Purchase>
@{
    ViewData["Title"] = "Committed Purchases";
}

<h2>Committed Purchases</h2>

@if (Model == null || !Model.Any())
{
    <p>No committed purchases are available.</p>
}
else
{
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Book ID</th>
                <th>Quantity</th>
                <th>Purchase Date</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var purchase in Model)
            {
                <tr>
                    <td>@purchase.Id</td>
                    <td>@purchase.BookId</td>
                    <td>@purchase.Quantity</td>
                    <td>@purchase.PurchaseDate.ToShortDateString()</td>
                </tr>
            }
        </tbody>
    </table>
}
